def reverse_an_array(a):
    n = len(a) ; left = 0 ; right = n-1
    while left < right:
        temp = a[left]
        a[left] = a[right]
        a[right] = temp
        left += 1
        right -= 1
    return a

print(reverse_an_array([1,2,3,4]))
    